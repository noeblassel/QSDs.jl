mesh Th=square(25,25);
plot(Th,wait=true);
varf a(u,v) = int2d(Th)(dx(u)*dx(v)+dy(u)*dy(v));
varf b(u,v) = int2d(Th)(u*v);

fespace Vh(Th,P1);
matrix A = a(Vh,Vh);
matrix B = b(Vh,Vh);

matrix C = A+B;

cout << C << endl;